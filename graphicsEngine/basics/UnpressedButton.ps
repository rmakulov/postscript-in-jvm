gsave
/q { gsave } bind def
/Q { grestore } bind def
/cm { 6 array astore concat } bind def
/w { setlinewidth } bind def
/J { setlinecap } bind def
/j { setlinejoin } bind def
/M { setmiterlimit } bind def
/d { setdash } bind def
/re { exch dup neg 3 1 roll 5 3 roll moveto 0 rlineto
      0 exch rlineto 0 rlineto closepath } bind def
/S { stroke } bind def
/f { fill } bind def
/g { setgray } bind def
/rg { setrgbcolor } bind def
%%EndProlog


%/proc exch def
/label exch def
/h exch def
/w exch def
/y exch def
/x exch def
%/x 50 def
%/y 50 def
%/w 400 def
%/h 100 def
%/label (Button) def


x y translate

q q
%fill all the button
0.801961 g
0 h w  h neg re f

%main frame of button
0.501961 0.45098 0.501961 rg
0.8 setlinewidth
0 J
0 j
[] 0.0 d
0 0 w h re S Q

%two shadows
%/shadowwidth h w add 200  idiv  def
/shadowwidth 4  def
/shift 2 def
0.301961 g
shift 0 w shadowwidth neg re f
w h shift sub shadowwidth h neg re f

%showing
/Times-Roman findfont 40 scalefont setfont
w 3 div h 3 div translate
0 0 moveto 1 0 0 setrgbcolor
label show

Q
%%Trailer
%%EOF
grestore


