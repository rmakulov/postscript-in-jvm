%PROLOG
/window <</label (window) /x 0 /y 0 /height 843 /width 651 /proc {} /parent null /children null /eType null>> def
/addElement {gelements gelements /maxElementNumber get 3 2 roll put  gelements /maxElementNumber gelements /maxElementNumber get 1 add put } def
%/maxElementNumber 0 def
/button {
    /proc exch def
    /parent exch def
    /label exch def
    /h exch def
    /w exch def
    /y exch def
    /x exch def
    /paintProc {(/home/user/dev/IdeaProjects/postscript-in-jvm/graphicsEngine/basics/UnpressedButton.ps) (r) file run} def
    /buttonDict <</label label /x x /y y /h h /w w /proc {proc}  /parent parent /children null  /paint {paintProc} /eType /CLICK >> def
    buttonDict addElement
    parent /children [buttonDict parent /children get] put
    x y h w label paintProc
} def
%END_PROLOG

/gelements 1 dict def
gelements /maxElementNumber 0 put
window addElement



