(graphicsEngine/basics/events/keyCodes.ps) (r) file run
%/snapshot save def
/evType exch def
/value exch def

/element gelements /focusedElement get def
/eventProcs element /eventProcs get def
 eventProcs evType known
{
    element value eventProcs evType get  execList
}if
